## 1. 服务层即时入库实现

- [x] 1.1 在 `MaterialService.CreateMaterialAsync` 中实现创建成功后的本地 upsert（按 Id 存在则更新，不存在则插入）。
- [x] 1.2 在 `ProviderService.CreateProviderAsync` 中实现创建成功后的本地 upsert（按 Id 存在则更新，不存在则插入）。
- [x] 1.3 保持原有异常处理语义：远端创建失败仍抛业务异常，本地写入失败需有清晰日志与异常边界。

## 2. 验证与回归

- [x] 2.1 增加/更新测试：覆盖 Material、Provider 创建成功后可被本地查询立即命中的场景。
- [x] 2.2 验证并发/重复创建下不会产生主键冲突导致流程失败（幂等分支生效）。
- [ ] 2.3 手工回归人工称重新增材料/供应商后立即搜索与选择流程，确认无需等待轮询同步。
