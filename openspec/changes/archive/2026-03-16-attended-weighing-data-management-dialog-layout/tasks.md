## 1. 对话框结构与触发

- [x] 1.1 在 `AttendedWeighingWindow.axaml` 中为“数据管理”按钮新增命令/事件绑定入口，用于打开数据管理对话框。
- [x] 1.2 新建数据管理对话框视图（例如 `DataManagementDialog`），包含基础容器和关闭按钮，并集成到现有对话框/窗口管理机制中。

## 2. 查询区与表格布局

- [x] 2.1 按照 `Samples/Index.xaml` 和截图在对话框顶部绘制查询条件区域（运单编号、发货单号、商品名称、状态、起止日期等控件），确保控件排列和对齐接近示例。
- [x] 2.2 在对话框中部实现台账表格布局，配置列头文本、列顺序、列宽和对齐方式，覆盖运单号、车牌号、类型、商品、状态、运单量、扣量、实际数量、实际重量、单位换算、进场时间等字段。

## 3. 分页区与测试数据

- [x] 3.1 在对话框底部绘制分页和统计信息区域，包括首页/上一页/下一页/末页按钮、跳转页控件以及当前页/总页数/总条数显示，与截图布局保持一致或相近。
- [x] 3.2 在对话框对应的 ViewModel/代码隐藏中创建至少一条本地测试台账记录集合，绑定到表格控件以渲染示例行，用于验收样式和数值格式。
