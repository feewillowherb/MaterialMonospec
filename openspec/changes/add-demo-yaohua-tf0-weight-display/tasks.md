## 1. 分支

- [x] 1.1 首次修改 MaterialClient 前，从该仓 trunk 切出并切换到 `add-demo-yaohua-tf0-weight-display`（Mode A）。不改生产称重项目。

## 2. 帧解析

- [x] 2.1 在 `MaterialClient.Demo` 增加命名 `record` 与 `TryParse`：12 字节、`02`…`03`、符号、6 位、小数位 0–4、第 2–9 字节异或校验；失败返回 null。不使用 tuple，不注册 DI。
- [x] 2.2 显示重量 = 带符号的 6 位整数 / `10^小数位`。`02 2D 30 30 30 31 33 30 32 31 44 03` 必须得到 `-1.30`。第 10–11 字节只做校验，不当毛/净标志。

## 3. Demo 窗口

- [x] 3.1 新增试窗，四个标题字面为 **实时**、**毛重**、**皮重**、**净重**。从 `DemoMainWindow` 用按钮打开。code-behind，不引用 `TruckScaleWeightService`。
- [x] 3.2 Hex 文本「载入」扫描全部合法帧；「下一帧」推进当前帧并刷新实时。残帧与校验失败不改实时，并用短状态说明接受/丢弃数量。
- [x] 3.3 未记账：皮重显示 `0`，毛重与净重跟随实时。「记毛重」「记皮重」冻结当时实时；都记下后净重 = 毛重 − 皮重（可为负）。「清空」回到未记账。未冻结的一侧仍跟随换帧后的实时。

## 5. tF=1 独立窗口

- [x] 5.1 在 `MaterialClient.Demo` 另开窗口，不与 tF=0 窗口共用。仪表须为 `tF=1`。固定 9600 8N1。地址默认 A（01）。按钮发握手 A、读毛重 B、读皮重 C、读净重 D；连续查询轮询 B/C/D。应答填入毛重、皮重、净重。校验失败仍显示原始 hex。

## 4. 肉眼验收

- [x] 3.4 窗口提供端口下拉、刷新、连接、断开。打开时固定 9600、8 数据位、无校验、1 停止位，不提供这些参数的编辑。有效帧刷新实时；校验失败不改实时。关闭窗口或断开时关闭端口。
- [ ] 4.1 启动 `MaterialClient.Demo`，打开该窗口。把对照步骤留给用户：选端口并连接后看实时变化；或载入 `02 2D 30 30 30 31 33 30 32 31 44 03` 应看到实时/毛重/净重 `-1.30`、皮重 `0`。Agent 不自行宣布验收通过。
