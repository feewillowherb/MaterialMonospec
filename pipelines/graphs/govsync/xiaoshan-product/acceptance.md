# Acceptance — xiaoshan-product

Status: **pending**

Agent 不得将本文件改为通过。用户回复 `pass` / `fail` 后，只改**本次 run** 下的副本。

| 项 | 值 |
|----|-----|
| run | |
| L0 | pending |
| L1 | pending |
| L2 | pending |
| L3 | pending（仅用户） |
| 对象 | 萧山成品 `inoutRecord/save`，`buildLicenseNo = L-02` |
| 原因 | |

### 验收提示

- L0：HTTP 有响应
- L1：JSON 含 `code`/`msg`
- L2：`code == 200`
- L3：成品虚拟场地是否被平台接受（设计稿 Q2）— **仅用户**
