# Acceptance — xnxs20240725003-weighbridge

Status: **pending**

Agent 不得将本文件改为通过。用户回复 `pass` / `fail` 后，只改**本次 run** 下的副本。

| 项 | 值 |
|----|-----|
| run | |
| L0 | pending |
| L1 | pending |
| L2 | pending |
| L3 | pending（仅用户） |
| 对象 | 接入码 `XNXS20240725003` 地磅 `lantu/saveRecord` |
| 原因 | |

### 验收提示

- L0：HTTP 有响应
- L1：JSON 含 `code`/`msg`
- L2：`code == 200`
- L3：称重记录是否可接受（车牌 `浙A12345`、重量 `1385`、接入码原值未拼 `-02`）— **仅用户**
