# Copy to secrets.local.yaml (gitignored). Never commit secrets.local.yaml.
# Optional overrides only — no passwords required for this Graph.

# sourceDb: _tmp/UrbanManagement.db
# baseUrl: http://127.0.0.1:44371
skipConfirm: false
