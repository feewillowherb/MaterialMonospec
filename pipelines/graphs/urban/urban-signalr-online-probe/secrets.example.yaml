# Copy to secrets.local.yaml and fill. Never commit secrets.local.yaml.

# Override when UM / Urban already bound to other ports:
# umBaseUrl: "http://127.0.0.1:44371"
# urbanDiagnosticBaseUrl: "http://localhost:9961"

# Optional Redis override:
# redisHost: "127.0.0.1"
# redisPort: 6379

# Optional: enable presence-hold step for this machine
# skipPresenceHold: false
# presenceHoldSeconds: 150
