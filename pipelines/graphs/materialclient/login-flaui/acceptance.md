# Acceptance — materialclient/login-flaui

Status: **pending**

Agent 不得将本文件改为通过。用户回复 `pass` / `fail` 后，只改**本次 run** 下的副本。

| 项 | 值 |
|----|-----|
| run | |
| L0 | pending |
| L1 | pending |
| L2 | pending |
| L3 | pending（仅用户） |
| 对象 | MaterialClient LoginWindow（FlaUI / windows MCP） |
| 原因 | |

### 验收提示（人读）

- L0：能定位登录窗与账号/密码/立即登录
- L1：已点击登录且非卡死在空操作
- L2：离开登录壳进入主称重窗，无明显登录错误文案
- L3：该账号业务权限/现场是否可接受 — **仅用户判定**
