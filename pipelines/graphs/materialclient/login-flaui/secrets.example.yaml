# Copy to secrets.local.yaml and fill. Never commit secrets.local.yaml.
# Keys required by config.yaml / design-brief secretsKeys.

username: ""
password: ""

# Optional absolute path to MaterialClient.exe when no LoginWindow is already open.
# exePath: "D:\\path\\to\\MaterialClient.exe"
