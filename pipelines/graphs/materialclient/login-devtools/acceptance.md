# Acceptance — materialclient/login-devtools

Status: **pending**

Agent 不得将本文件改为通过。用户回复 `pass` / `fail` 后，只改**本次 run** 下的副本。

| 项 | 值 |
|----|-----|
| run | |
| L0 | pending |
| L1 | pending |
| L2 | pending |
| L3 | pending（仅用户） |
| 对象 | MaterialClient LoginWindow（avalonia_devtools） |
| 原因 | |

### 验收提示（人读）

- L0：attach-to-app 成功并定位 LoginWindow / ViewModel
- L1：Username/Password 已写入且 LoginCommand 已触发
- L2：`IsLoginSuccessful` 或主称重窗出现，无持续错误文案
- L3：该账号业务是否可接受 — **仅用户判定**
