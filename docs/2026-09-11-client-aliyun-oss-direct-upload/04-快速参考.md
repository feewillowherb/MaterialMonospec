# 04 — 快速参考

## 今日谁上传到哪

| 谁 | 传到哪 | 凭据在哪 |
|----|--------|----------|
| Material 主程序 | Aliyun OSS `PutObject` | 客户端 `AliyunOss` secret |
| MaterialClient.Urban | UM `Uploads/` | 无 OSS；业务 API |
| UM 自身 | 本地盘 | 无 OSS SDK |

## 目标（Urban）

```text
客户端 ──ticket──► UM(STS) ──临时钥──► 客户端 ──PutObject──► OSS
客户端 ──complete(key)──► UM 登记 Guid ──► Receive 照旧
```

客户端 appsettings：**不出现** OSS Endpoint/Bucket/AK/SK。

## 关键路径

| 用途 | 路径 |
|------|------|
| OSS 配置类 | `repos/MaterialClient/src/MaterialClient.Common/Configuration/AliyunOssConfig.cs` |
| OSS 上传 | `.../Services/OssUploadService.cs` |
| Material 同步编排 | `.../Services/AttachmentService.cs` |
| Urban 同步 | `repos/MaterialClient/src/MaterialClient.Urban/Services/UrbanAttachmentSyncService.cs` |
| UM 落盘 | `repos/UrbanManagement/src/UrbanManagement.Core/Services/FileService.cs` |
| UM 附件实体 | `.../Entities/AttachmentFile.cs`（仅 LocalPath） |
| UM multipart | `.../App/Controllers/UrbanAttachmentController.cs` |

## 决策勾选

- [ ] 签发方：UM / 独立服务 / BasePlatform  
- [ ] 模式：STS+SDK（推荐） / 预签名 PUT  
- [ ] Bucket：共用 Material桶+前缀隔离 / Urban 独立桶  
- [ ] UM 读图：GetObject / 本地镜像 / 预签名 GET  
- [ ] 旧 multipart：并存多久  
- [ ] 是否同批清 Material 客户端长期 Key  

## 禁区

- 客户端 secret 再写一套长期 `AliyunOss`  
- 只改 Urban 上传、不改 UM 读图（政务/审批会断）  
- 未 OpenSpec 直接改主路径行为  
