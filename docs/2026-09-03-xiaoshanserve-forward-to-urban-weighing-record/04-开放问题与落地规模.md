# 04 · 开放问题与落地规模

## 已关闭

| # | 决策 |
|---|------|
| — | 转换业务核心在 **`LegacyGovSyncAppService`**（见总览 D1）；不在 Controller / XiaoShanServe |
| — | 用枚举 **`UrbanWeighingIngestSource`**（`Modern`/`Legacy`/`Migrated`）区分入站（D4）；细节 [05](./05-入站来源枚举.md) |
| — | XiaoShanServe 历史批迁（D5 / 原 06） | ⏸ [INT-007](../intake/2026-09/INT-007-xiaoshanserve-govsyncdata-migrate.md) |

## 开放问题（实现前需确认）

| # | 问题 | 默认建议（可推翻） | 落点 |
|---|------|-------------------|------|
| Q1 | 对外 `/Api/Post` 由谁托管？ | UM 直绑或反代到 UM | 运维切流，非 Service 内逻辑 |
| Q2 | `ClientRecordId` 如何生成？ | 确定性 Guid（AccessCode+snapTime+carNo+deviceID+weight） | **LegacyGovSyncAppService** |
| Q3 | 多图 `AttachType` 如何切分？ | 全部 `UrbanPhoto` | **LegacyGovSyncAppService** |
| Q4 | 凡东码过渡？ | 若需要，仅在 Service 内换码；UM 实体不落列 | **LegacyGovSyncAppService** |
| Q5 | XiaoShanServe 出站？ | 切流后停用；历史 `GovSyncData` 只读 | 运维 |
| Q6 | Legacy 端点鉴权？ | 与现网旧接口一致或网关层；Service 内不重复造会话 | Controller / 宿主 |
| Q8 | 枚举/属性最终命名？ | `UrbanWeighingIngestSource` / `IngestSource` | Entity + migration |
| Q10 | 对外 Receive 是否采纳客户端传入的来源？ | **否**；仅 Legacy / 批迁内部可写非 Modern | `ReceiveAsync` |
| Q11–Q16 | 批迁细节（Migrated、缺图、Sync 映射、拆 change…） | 见 [06](./06-XiaoShanServe历史数据迁移.md) | 迁移工具 |

## 落地规模（effort-token-estimate）

| 切片 | 档位 | 说明 |
|------|------|------|
| 仅调研本夹 | — | 已完成 |
| `LegacyGovSyncAppService` 实装（映射+附件+Receive+`IngestSource`+测试） | **M–L** | 单仓 UrbanManagement；含枚举/migration |
| XiaoShanServe `Gov_SyncData` → UM 批迁 | ⏸ | [INT-007](../intake/2026-09/INT-007-xiaoshanserve-govsyncdata-migrate.md)；本期不做 |
| 同机切流 + 防双报联调 | **S–M** | 反代/停 Serve 出站；无第二套转换代码 |
| 端到端（含政府出站回归） | **L–XL** | 需人闸；可借 `pipelines/graphs/govsync/*` 作对照，**非**本需求实现载体 |

开 OpenSpec 后：effort **只**写入 change 的 `.openspec.yaml`，勿写入 `proposal.md`。

## 建议验收要点（供后续 tasks，非宣布通过）

- [ ] 新行带正确 `IngestSource`（在线：Legacy / Modern；Migrated 随 INT-007）  
- [ ] （挂起）批迁验收见 INT-007 / [06](./06-XiaoShanServe历史数据迁移.md) — **本期不执行**  
- [ ] `POST` 合法 AccessCode + 图 → UM 存在 `UrbanWeighingRecord`，`AccessCode`/`ProId`/`TotalWeight` 正确  
- [ ] 在线 Legacy：同幂等键重试不产生第二行（非 Empty `ClientRecordId`）；**不**改写已有 `IngestSource`  
- [ ] `AttachmentFile` / join 行存在；实体无路径字符串  
- [ ] **无**新 `GovSyncData` 行  
- [ ] 政府侧仅 UM Worker 出站一次（同机抓包或 sync 状态）  
- [ ] 旧客户端仍认 `success`/`code` 形状  

## 与既有材料关系

| 材料 | 关系 |
|------|------|
| INT-006 | 本需求的正式消化入口；本夹作代码证据 |
| entity 语义化 D18/D19 | 约束实现边界（禁双写） |
| `urban-weighing-api` spec | Receive / TotalWeight kg / AccessCode 映射权威 |
| 萧山地磅 SyncDoc / govsync graphs | **出站**协议；本夹关注 **入站到 UM**，勿把 probe Graph 当实现 |

## 下一步动作（人工选择）

1. 确认 Q2–Q4、Q8/Q10 及 Q1 切流（批迁 Q11–Q16 随 INT-007 挂起）  
2. `/opsx:propose`：在线 INT-006；**勿**消化 INT-007  
3. **勿**另开「XiaoShanServe 内转换」；**勿**把枚举/批迁塞进 `fix-urban-weighing-receive-legacy-compat`
