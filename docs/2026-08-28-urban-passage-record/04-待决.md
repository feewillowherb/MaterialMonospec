# 04 · 已决与仍待决

## 已决（2026-08-28）

1. **车牌颜色**：SDK 无则默认 **「无」**。不是设置项。  
2. **车型**：SDK 无则默认 **「大车」**。不是设置项。  
3. **抓拍图**：正常情况下海康会给两张。入库 **有则存、无则不补**（不复制凑双图、不要求必须两张）。  
4. **上报**：本期 **不实现** 萧山 `inoutRecord/save` 或任何上传；只到 **Urban 客户端入库 + 列表**。

## 仍可拍板（不挡 propose）

- **全部记录** UX：混合一张表 vs 称重下列进出块。  
- **未识别车牌** 存「无」后，列表是否原样显示。
