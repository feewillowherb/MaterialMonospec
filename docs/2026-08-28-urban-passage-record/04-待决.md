# 04 · 已决与仍待决

## 已决（2026-08-28）

1. **车牌颜色**：SDK 无则默认 **「无」**。不是设置项。  
2. **车型**：SDK 无则默认 **「大车」**。不是设置项。  
3. **抓拍图**：正常情况下海康会给两张。入库 **有则存、无则不补**。  
4. **上报**：本期 **不实现** 任何上传。  
5. **全部记录**：一张 **混合表**（不是称重下列进出块）。右侧照片 **只放大图**，小图不进 UI。列见 [05](./05-列表列设计.md)。  
6. **专用 tab**：只展示进出自身字段（颜色、车型、进出、场地等），不要称重列。  
7. **未识别车牌**：库内仍为「无」；**列表车牌列显示「未识别」**，不原样展示「无」。颜色列的「无」可原样显示。

## 仍可拍板（不挡 propose）

无。正常/异常 tab 保持只称重，已写入 05。
