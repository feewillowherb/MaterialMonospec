# 测试矩阵与缺口

## A. 本地卡口 / 成品（当前可测）

| # | 步骤 | 通过判据 |
|---|------|----------|
| A1 | Urban 设置两行 LPR：`SiteType=Checkpoint` / `FinishedProduct`，记下 `Name` | 保存成功；能力上有卡口/成品 tab |
| A2 | `MinimalWebHost:EnableOnStartup=true`，启动 Urban | `GET :9961/` 有 `test-plate` |
| A3 | POST test-plate，`deviceName`=卡口名 | 卡口 tab 新行；库表 `UrbanPassageRecords.PassageSource=Checkpoint` |
| A4 | POST test-plate，`deviceName`=成品名 | 成品 tab 新行；`PassageSource=FinishedProduct` |
| A5 | POST 使用不存在的 `deviceName` | 无进出记录（handler 静默 return） |
| A6 | POST 对准 `SiteType=Scale` 的设备名 | 无进出记录（地磅路径可能仍吃事件） |

**不依赖** Hikvision 真机；**不依赖** HCNetSDK DLL 加载成功（事件层注入）。

## B. 海康解析回归（无 UI）

| # | 步骤 | 通过判据 |
|---|------|----------|
| B1 | `dotnet test` 含 `HikvisionPlateGolden*` / `HikvisionLprService*` | 绿 |

## C. 真机抓拍（可选）

| # | 步骤 | 通过判据 |
|---|------|----------|
| C1 | 设备在线 + 设置页测试抓拍 / 被动过车 | 真机回调 → 同 A3/A4 落库 |

## D. 客户端 → UM HTTP 上云（当前缺口）

OpenSpec `add-urbanmanagement-passage-xiaoshan-upload`：

- [ ] 7.1 进出附件 multipart  
- [ ] 7.2 卡口 pending → checkpoint ingest；成品 → product ingest  

现状：

- **UM**：`UrbanCheckpointPassageAppService.ReceiveAsync` / `UrbanFinishedProductPassageAppService.ReceiveAsync` 已落地  
- **MC Urban**：`IUrbanManagementApi` 仅有称重 `receive` + 附件；**无** passage Refit；`UrbanServerUploadService` / `PollingBackgroundService` 只扫称重 pending  

因此：

| 问题 | 答案 |
|------|------|
| 卡口/成品是否满足「用 HTTP POST 测客户端→UM」？ | **本地注入满足**；**客户端上云 HTTP POST 尚未接好** |
| 如何先测 UM Receive？ | Postman/curl 直打 UM AppService（ABP 惯例 `/api/app/...`），与 MC 解耦 |

## E. Mock 类注意

`MockHikvisionLprService.SimulatePlateRecognition` → MessageBus，**不能**驱动 `UrbanPassageLprHandler`。单测进出应：

- 直接调 `UrbanPassageRecord.FromLprCapture` / `CreateFromLprAsync`，或  
- 向真实 `ILocalEventBus` 发布 `LicensePlateRecognizedEventData`（集成测）

## 推荐联调顺序

1. A1–A4（本机 test-plate）确认卡口/成品分流  
2. B1 保证解析层不回归  
3. UM Receive + Gov 出站用 pipelines / Postman（与 MC 并行）  
4. 待 §7 实现后再加「MC 上传 pending 进出」用例
