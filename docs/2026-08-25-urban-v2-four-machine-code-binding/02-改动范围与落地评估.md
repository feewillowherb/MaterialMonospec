# 02 — 改动范围与落地评估

## 1. 目标（已确认）

| 场景 | 结论 |
|------|------|
| **A. 多机并存（同时在线称重）** | **已选定**（继承 2026-08-06；上限升为 **4**） |
| B. 主备切换 | 不采纳 |
| C. 仅预登记 | 不作为本期方案 |
| D. 改写存量 5001 为多槽 | **不采纳**；5001 保持单机覆盖 |

选定方案：

- **新增 ProductCode（Urban V2）**
- 项目级 **设备1…设备4** 四槽（场地 ↔ 槽 1:1）
- F4 改为成员校验；每台 JWT 仍绑定本机码；发码选槽覆盖写入
- AccessCode / AuthEndTime 项目级共享；**本期无**解绑 UI/API，但模型/契约按 **D37 预留**后期槽位解绑

设计母本见 [归档双机调研](./archive/2026-08-06-urban-dual-machine-code-binding/00-调研总览.md)。

## 2. 已确认产品决策（Decision Log）

| # | Decision | 结论 | 日期 |
|---|----------|------|------|
| D1 | 目标场景 | 最多 4 台同时在线称重 | 2026-08-06（2）→ **2026-08-25（4）** |
| D2 | 槽位与发码 | 生成激活码时选择 **设备1…设备4**；激活写入对应槽 | 同上演进 |
| D3 | 换机策略 | 槽已占用时用该槽新码激活 = **覆盖该槽**；其它槽不动 | 2026-08-06 继承 |
| D4 | AccessCode / AuthEndTime | **四槽共享**同一项目 `AccessCode` 与 `AuthEndTime` | 继承 |
| D5 | 解绑 / 清空 | **本期不做** UI/API；换机靠发码覆盖；**后期必做**；本期预留扩展（D37） | 2026-08-25 修订（相对双机「永不做」） |
| D37 | 后期解绑预留 | 槽可空、按 Slot 寻址、契约可加 Unbind；本期不实现 | **2026-08-25 新增** |
| D6 | 产品形态 | **新增城管 ProductCode V2**；**不**改写 5001 全局行为 | **2026-08-25 新增** |
| D7 | 绑定上限 | 每项目最多 **4** 个机器码 / **4** 个场地槽 | **2026-08-25 新增** |

### 实现默认（可写入 design，非阻塞）

| 项 | 默认 | OpenSpec ID |
|----|------|-------------|
| V2 `ProductCode` 数值 | **待定**（候选 `5002`；锁定后写入 OpenSpec / 枚举） | D8 |
| 5001 存量数据 | **不迁移**到 V2；V2 为新开授权行 | D31 |
| 同槽同机再激活 | 幂等成功（刷新 JWT） | D18 |
| 同机已在另一槽又占新槽 | 建议拒绝（避免浪费名额） | D17 |
| 场地元数据 | 本期槽位即场地名额；场地显示名可选二期 | D32 |

开 OpenSpec 时的完整 Decision 清单（含 API / JWT / F4 / Client 分叉）：见 [03-openspec-decision-checklist.md](./03-openspec-decision-checklist.md)。

## 3. 方案对比

### 方案 1：新 ProductCode + 四槽 + 发码选槽（已选定）

**模型**：Urban V2；设备1…4；每台 JWT 只含本机 `machineCode`；验签 `jwtMachineCode ∈ {槽1…槽4}`。

| 层 | 改动要点 |
|----|----------|
| **BasePlatform** | 注册 V2 产品；四槽存储（优先可空槽 / 子表，利后期解绑）；发码 UI 必选槽 1–4，展示各槽当前码（遮掩）；Redis 载荷带 `Slot`；激活 API 支持 V2 且按槽覆盖；`BuildLicenseFile`/`RefreshJwt`：请求机签发且 ∈ **非空**四槽；**本期不做**解绑 UI，**预留**按 Slot 清空扩展；**5001 路径保持单字段覆盖** |
| **UrbanManagement** | 识别 V2 ProductCode；`GovProject`（或并列结构）同步四槽（可空）；F4：V2 走成员包含（仅非空槽），5001 仍相等；激活代理按槽 upsert；Catalog 拉取允许 V2；AccessCode/AuthEndTime 仍单字段共享 |
| **MaterialClient** | 枚举/设置增加 Urban V2；激活带 V2 ProductCode；本地仍存本机码；仅本机所在槽被覆盖时 DeviceChanged；`StaticLicenseChecker` 仍比对本机 claim |
| **OpenSpec** | 新/改 specs：urban V2 auth、F4、catalog（`ProductCode ∈ {5001, V2}` 或分端点） |

**优点**：多场地并存；5001 客户零行为变化；换机目标明确；本期避开解绑权限流，后期可另开 change。  
**缺点**：跨三仓 + 新产品注册；发错槽会覆盖错机；Catalog/同步边界要显式接纳 V2。

### 方案 2：直接把 5001 扩成四槽（已不采纳）

与「新增一种授权 V2」冲突；存量项目语义突变，迁移与灰度成本高。

### 方案 3：复用 `Material_MachineCode` 多行表（备选存储形态）

可作为 V2 四槽的存储实现之一；须与激活 / LicenseFile / Catalog / F4 全链路打通，并与物料 `AuthType` 隔离，避免串表。

### 不推荐的捷径

- 仅改客户端或绕过 `StaticLicenseChecker`。  
- 逗号拼接多码塞进现有单字段而不改签发/验签。  
- 本期交付完整解绑 UI/API（属后期 change；与 D5 冲突）。  
- 做成「槽永不可空 / 无法按 Slot 清空」的死结构（与 D37 冲突）。  
- 只改 UM/Client、不动 BP 权威源。

## 4. 可落地改动清单

### 4.1 BasePlatform（阻塞依赖，优先）

- [ ] `JC_Product` / `ProductExtEnum`：注册 Urban V2 ProductCode（数值待定）  
- [ ] 数据模型：V2 四槽（字段或子表）；与 5001 单字段路径隔离  
- [ ] 发码 UI：V2 必选设备1…4；展示各槽当前机器码（遮掩）  
- [ ] Redis 激活码载荷：`Slot` + V2 `ProductCode`  
- [ ] 激活：`ActivateUrban` 扩展或 `ActivateUrbanV2`；按 `Slot` 覆盖；**本期不提供**清空 API，但槽模型支持空值（D37）  
- [ ] `LicenseFile` / `RefreshJwt`：按请求 `machineCode` 签发，校验 ∈ **非空**四槽  
- [ ] AccessCode / AuthEndTime：项目级单值，四槽共享  
- [ ] 项目目录 API：纳入 V2（至少一个槽非空即可入目录；兼容后期解绑后槽空）  
- [ ] design 注明后期 `UnbindSlot`（或等价）扩展点；**不**进本期 tasks 实现  
- [ ] **回归**：5001 仍单机覆盖、按钮与下载条件不变

### 4.2 UrbanManagement

- [ ] 常量/选项：支持 V2 ProductCode（与 5001 并存）  
- [ ] `GovProject`：四槽存储 + 迁移（仅 V2 项目使用；5001 仍单字段或槽1）  
- [ ] 激活代理：按槽同步，禁止无脑覆盖其它槽  
- [ ] `JwtAntiTamperService` F4：V2 成员包含；5001 相等  
- [ ] `RefreshJwt`：使用客户端 JWT 中的 machineCode  
- [ ] `GovProjectPullManager`：接受 catalog 中的 V2  
- [ ] 单测：四机均 Pass；覆盖一槽旧机 Fail、其它槽仍 Pass；5001 回归  
- [ ] **本期不做**解绑/清空 API 或 UI；同步与 F4 按「非空槽」集合，为后期解绑留空槽语义

### 4.3 MaterialClient

- [ ] ProductCode / WeighingMode（若需要）映射 Urban V2  
- [ ] 激活请求携带 V2 ProductCode；错误文案（槽满/发错槽/签发失败）  
- [ ] 设备吊销：仅本机所在槽被覆盖时触发  
- [ ] 项目信息 UI（可选：本机码 / 已占用 n/4）  
- [ ] 回归：5001 启动门闩、恢复窗、上传 `SubmitMachineCode`

### 4.4 Monospec / OpenSpec

- [ ] `proposal` / `design` / delta specs / `tasks`（主仓库 `openspec/`）  
- [ ] Impact：BasePlatform、UrbanManagement、MaterialClient  
- [ ] design 写入 D1–D7、**D37** + 已收口 D8+（对照 [03](./03-openspec-decision-checklist.md)）；effort 仅进 `.openspec.yaml`

## 5. 仍开放的次要项

| 项 | OpenSpec ID | 说明 |
|----|-------------|------|
| V2 ProductCode 数值 | D8 | 与 BP 产品表、客户端枚举一次锁定 |
| 存储形态 | D9 | 四列 vs 子表（推荐子表/多行） |
| Catalog 对外 | D10 | 外部系统是否需感知四槽字段 |
| 激活 API | D11 | 扩展 vs 新建 |
| 同项目 5001/V2 | D20 | 并存 vs 互斥 |
| 客户端展示 | D29 | 「已占用 n/4」可二期 |
| 同机占两槽 | D17 | 拒绝 vs 允许（建议拒绝） |
| 场地显示名 | D32 | 是否在槽上挂 `SiteName`（本期可不做） |

其余 JWT / F4 / Out of Scope 分叉见 [03](./03-openspec-decision-checklist.md) 的 C/D/E 节。

## 6. 风险评估

| 风险 | 等级 | 说明 |
|------|------|------|
| BasePlatform 未改、只改 UM/Client | 高 | 权威仍单字段，四机无法成立 |
| V2 数值未统一导致激活/目录过滤失败 | 高 | 枚举 / JC_Product / Catalog / 客户端必须同日锁定 |
| 误把 5001 路径改成四槽 | 高 | 破坏存量；须分支清晰 + 回归用例 |
| 发错槽覆盖错机 | 中 | 发码页展示当前槽绑定可缓解；本期无解绑则只能再发码覆盖回来（后期解绑可缓解） |
| 本期模型不支持空槽 | 高 | 会堵住后期解绑；须落实 D37 |
| Catalog 丢槽 / 只认 5001 | 中 | 拉取会把 V2 项目漏同步或打回单码 |
| `RefreshJwt` 仍用权威单码签发 | 高 | 其它槽机拿到错误 claim → StaticLicenseChecker 失败 |
| 会话低估「新产品 + 四槽 + 三仓联调」 | 中 | 建议按仓拆会话（XL） |

## 7. 落地规模（effort，token 口径）

| 块 | 档位参考 |
|----|----------|
| OpenSpec 提案 / design / specs | M |
| BasePlatform：新产品 + 四槽 + 发码 + 激活 + 签发 + Catalog | L |
| UrbanManagement：数据 + F4 分支 + 激活同步 + 拉取 + 测试 | L |
| MaterialClient：ProductCode + 激活/吊销/展示 + 回归 | M |
| 联调（多机真机 + 槽覆盖 + 5001 回归） | L |
| **合计** | **XL**（约 100万+ token；跨仓鉴权主路径，建议分仓/分阶段会话） |

**可行性结论**：

- 技术可落地；组织上依赖 BasePlatform 先落地 V2 产品与四槽权威。  
- MaterialClient 单独改无法完成。  
- 验收以「V2 发码选槽覆盖 + 最多 4 机并存 + 5001 行为不变 + **本期无解绑 UI** + **模型可空槽（D37）**」为准。

## 8. 建议落地节奏

1. 主仓库开 OpenSpec，写入 D1–D7 及 [03](./03-openspec-decision-checklist.md) 中已收口项，`.openspec.yaml` 标注 `effort.tier: XL`。  
2. 与 BP 锁定 **D8–D11**（ProductCode / 存储 / Catalog / 激活 API）。  
3. 先实现 BP V2 + 四槽 + 发码选槽 + 按请求机签发，再改 UM F4 分支，最后客户端。  
4. 验收用例至少覆盖：  
   - 四槽先后激活后，已激活机均能上传  
   - 激活槽 N **不**导致其它槽 DeviceChanged  
   - 再发槽 1 码换机 A→E 后：E 可用、A 吊销、槽 2–4 仍可用  
   - AccessCode / AuthEndTime 各机一致  
   - **5001** 仍后激活覆盖、第二机踢第一机  
   - **本期无**解绑/清空入口；槽位允许空（为后期解绑预留）  
   - 第 5 台无法获得第五槽（发码仅 1–4）

## 9. 对齐说明

对应改动核心：

**在新增 Urban V2 ProductCode 上，把「唯一权威机器码 + 全局覆盖 + 严格相等」改成「设备1…4 四槽 + 发码选槽覆盖 + 成员校验（非空槽）+ 按请求机签发 JWT；AccessCode/AuthEndTime 共享；本期无解绑 UI，但槽可空并预留后期 Unbind」。存量 5001 保持单机覆盖。**

客户端改动相对可控；**BasePlatform 新产品注册与四槽权威是主体。**
